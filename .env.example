# Supabase (recommended): Project Settings > Database > Connection string
# Use "Transaction" pooler (port 6543) for serverless/Vercel.
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require

# Optional: require Bearer token when calling POST /api/sync (cron can send this)
# CRON_SECRET=your-secret

# Optional: use Hyperliquid testnet
# HYPERLIQUID_TESTNET=1
