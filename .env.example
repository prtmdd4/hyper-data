# Supabase: Project Settings > Database > Connection string (Transaction pooler, port 6543)
# This project's pooler: aws-0-us-east-1.pooler.supabase.com (ref: aptobaagvekszbfdkesp)
DATABASE_URL=postgresql://postgres.aptobaagvekszbfdkesp:iCLFQwVtNgGyiroy@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require

# Optional: require Bearer token when calling POST /api/sync (GitHub Actions cron sends this)
# If set, add the same value as CRON_SECRET in GitHub repo secrets so the workflow can authenticate.
# CRON_SECRET=your-secret

# Optional: use Hyperliquid testnet
# HYPERLIQUID_TESTNET=1
